# CLAUDE.md

Read [AGENTS.md](./AGENTS.md) before starting work in this repository, and treat it as a
required instruction source in every session.

It defines the repository structure, the file-vs-folder convention every module follows,
the tooling invariants, and the test layout. Nested `AGENTS.md` files, where they exist,
add module-scoped guidance on top of it.
