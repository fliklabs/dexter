"""Fixtures shared across more than one dexter module.

Module-specific fixtures belong in ``tests/<module>/conftest.py`` instead — pytest
scopes a ``conftest.py`` to its own directory subtree, which is what keeps each
module's test support local to that module.
"""
