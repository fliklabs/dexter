"""dexter — a collection of independent, reusable framework modules.

Import framework modules directly rather than from this package:

    from dexter.dependency_injection import Container

This module deliberately re-exports nothing, so ``import dexter`` never pulls in
every framework.
"""

__version__ = "0.1.0"
