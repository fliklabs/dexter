"""Runnable reference applications for dexter.

These are demonstrations, not tests. They assert nothing; they exist to make the library's
behaviour visible. They are type-checked under mypy strict and linted like the rest of the
repository, but nothing in the test suite executes them.

Run one with, for example::

    uv run python -m examples.taskflow
"""
